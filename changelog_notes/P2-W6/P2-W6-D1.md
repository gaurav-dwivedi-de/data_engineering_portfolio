# P2-W6 --- Daily Development Notes

## 24/09/2026

### Task

-   P2-W6 D1: Terraform Foundation + Oracle Cloud Provider.
-   Verify the OCI CLI installation and Oracle Cloud authentication.
-   Verify the installed Terraform version and OCI Terraform provider.
-   Validate the Terraform configuration.
-   Run a read-only Terraform plan against the existing Oracle Cloud
    environment.
-   Verify the final Terraform files.
-   Verify the Git working tree and latest committed Terraform change.
-   Record the  commands and terminal outputs used during D1.

### Why this task was required

P2-W6 introduces Terraform / Infrastructure as Code for the Weather
Intelligence Platform.

D1 establishes the Terraform foundation and validates connectivity to
the existing Oracle Cloud environment before Terraform is used to work
with the actual Oracle Cloud network and VM infrastructure in the
following tasks.

No `terraform apply` was performed during D1.

### Changelog

#### 1. Verify OCI CLI

The OCI CLI version was checked from the project root:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % oci --version
3.93.0
```

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % oci --version
```

The  output was:

``` text
3.93.0
```

#### 2. Verify Oracle Cloud authentication

Oracle Cloud region access was verified with:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % oci iam region-subscription list
{
  "data": [
    {
      "is-home-region": true,
      "region-key": "LHR",
      "region-name": "uk-london-1",
      "status": "READY"
    }
  ]
}
```

This confirmed that the OCI CLI could access the configured Oracle Cloud
region:

``` text
uk-london-1
```

with status:

``` text
READY
```

#### 3. Enter the Terraform directory

The Terraform directory was entered with the  command:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % cd terraform
```

The shell prompt then changed to:

``` text
(.venv) gaurav@gauravs-Mac-mini terraform %
```

#### 4. Verify Terraform and OCI provider

Terraform was checked with the  command:

``` text
(.venv) gaurav@gauravs-Mac-mini terraform % terraform version
```

The  output was:

``` text
Terraform v1.16.4
on darwin_arm64
+ provider registry.terraform.io/oracle/oci v9.2.0
```

Therefore the verified environment was:

``` text
Terraform v1.16.4
darwin_arm64
oracle/oci v9.2.0
```

#### 5. Verify Terraform formatting

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini terraform % terraform fmt -check
```

The command returned no output.

#### 6. Validate Terraform configuration

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini terraform % terraform validate
```

The  output was:

``` text
Success! The configuration is valid.
```

#### 7. Run Terraform plan

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini terraform % terraform plan
```

The  output was:

``` text
data.oci_identity_availability_domains.ads: Reading...
data.oci_identity_availability_domains.ads: Read complete after 0s [id=IdentityAvailabilityDomainsDataSource-699165265]

No changes. Your infrastructure matches the configuration.

Terraform has compared your real infrastructure against your configuration and found no differences, so no changes are needed.
```

This confirmed that Terraform successfully read the OCI
availability-domain data and that the configuration resulted in no
infrastructure changes.

#### 8. Verify Terraform files

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini terraform % find . -maxdepth 2 -type f | sort
```

The  output was:

``` text
./.terraform.lock.hcl
./README.md
./data.tf
./outputs.tf
./provider.tf
./versions.tf
```

The final D1 Terraform directory therefore contained:

``` text
terraform/
├── .terraform.lock.hcl
├── README.md
├── data.tf
├── outputs.tf
├── provider.tf
└── versions.tf
```

#### 9. Return to the project root

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini terraform % cd ..
```

The shell prompt returned to:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio %
```

#### 10. Verify Git status

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % git status
```

The  output was:

``` text
On branch main
Your branch is up to date with 'origin/main'.

nothing to commit, working tree clean
```

This confirmed that the Terraform changes were committed and the local
repository was synchronized with the remote repository.

#### 11. Verify the latest Git commit

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % git log -1 --oneline
```

The  output was:

``` text
0783a9a (HEAD -> main, origin/main, origin/HEAD) feat(terraform): initialize OCI Terraform config
```

This confirmed that the Terraform foundation was committed and pushed to
`origin/main`.

#### 12. Verify `versions.tf`

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % cat terraform/versions.tf
```

The  output was:

``` text
terraform {
  required_version = ">= 1.0"

  required_providers {
    oci = {
      source  = "oracle/oci"
      version = ">= 6.0"
    }
  }
}%
```

#### 13. Verify `provider.tf`

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % cat terraform/provider.tf
```

The  output was:

``` text
provider "oci" {
  config_file_profile = "DEFAULT"
}%
```

#### 14. Verify `data.tf`

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % cat terraform/data.tf
```

The  output was:

``` text
data "oci_identity_availability_domains" "ads" {
  compartment_id = "ocid1.tenancy.oc1..aaaaaaaarhdmxiuer7rn7aasjzq2xv2d5jotqnghsgsdggmzzqevav5pimya"
}%
```

#### 15. Verify `outputs.tf`

The  command used was:

``` text
(.venv) gaurav@gauravs-Mac-mini data_engineering_portfolio % cat terraform/outputs.tf
```

The command returned no content.

### Observations

#### 1. OCI CLI was operational

The  verified OCI CLI version was:

``` text
3.93.0
```

#### 2. Oracle Cloud authentication was operational

The OCI region subscription check returned:

``` text
region-name: uk-london-1
status: READY
```

#### 3. Terraform was operational

The  verified Terraform environment was:

``` text
Terraform v1.16.4
on darwin_arm64
+ provider registry.terraform.io/oracle/oci v9.2.0
```

#### 4. Terraform formatting passed

The  command:

``` text
(.venv) gaurav@gauravs-Mac-mini terraform % terraform fmt -check
```

returned no output.

#### 5. Terraform configuration validation passed

The  result was:

``` text
Success! The configuration is valid.
```

#### 6. Terraform plan made no infrastructure changes

The  plan result was:

``` text
No changes. Your infrastructure matches the configuration.

Terraform has compared your real infrastructure against your configuration and found no differences, so no changes are needed.
```

No `terraform apply` was executed during D1.

#### 7. Git repository was clean

The  Git status was:

``` text
On branch main
Your branch is up to date with 'origin/main'.

nothing to commit, working tree clean
```

#### 8. D1 Terraform configuration was committed

The  latest commit was:

``` text
0783a9a (HEAD -> main, origin/main, origin/HEAD) feat(terraform): initialize OCI Terraform config
```

### What P2-W6 D1 established

P2-W6 D1 established and verified the Terraform foundation for the
Weather Intelligence Platform.

The verified Terraform files were:

``` text
terraform/
├── .terraform.lock.hcl
├── README.md
├── data.tf
├── outputs.tf
├── provider.tf
└── versions.tf
```

The OCI provider was configured to use:

``` text
DEFAULT
```

through:

``` hcl
provider "oci" {
  config_file_profile = "DEFAULT"
}
```

The Terraform configuration successfully read the OCI
availability-domain data source.

The D1 validation sequence completed successfully:

``` text
OCI CLI verification       → Passed
OCI authentication         → Passed
Terraform version          → Passed
Terraform formatting       → Passed
Terraform validation       → Passed
Terraform plan             → Passed
Terraform file verification→ Passed
Git status                 → Clean
Git commit                 → Present on origin/main
```

### Final P2-W6 D1 Validation Summary

  --------------------------------------------------------------------------------------------------------------
  Validation Area          Evidence                                                 Result
  ----------------------- -------------------------------------------------------------- -----------------------
  OCI CLI                 `3.93.0`                                                       Passed

  OCI authentication      `uk-london-1` / `READY`                                        Passed

  Terraform               `v1.16.4` / `darwin_arm64`                                     Passed

  OCI provider            `oracle/oci v9.2.0`                                            Passed

  Terraform formatting    `terraform fmt -check` returned no output                      Passed

  Terraform validation    `Success! The configuration is valid.`                         Passed

  Terraform plan          `No changes. Your infrastructure matches the configuration.`   Passed

  Terraform files         Six files verified                                             Passed

  Infrastructure changes  No `terraform apply`                                           None

  Git working tree        `nothing to commit, working tree clean`                        Passed

  Git commit              `0783a9a ... initialize OCI Terraform config`                  Passed
  --------------------------------------------------------------------------------------------------------------

### Final Status

**P2-W6 D1 --- COMPLETE**

Terraform foundation and Oracle Cloud provider connectivity were
established and verified.

No Oracle Cloud infrastructure was provisioned or modified during D1.

The next task is:

**P2-W6 D2 --- Oracle Cloud Network Infrastructure**
